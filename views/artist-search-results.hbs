<div class="results-container">
    <h1>The Results are : </h1>
        {{#each searchResults}}
            <h2>{{name}}</h2>
            <img src="{{images.0.url}}" alt="{{name}}">
        {{/each}}
</div>